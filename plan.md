# Parallax Flow React Native rebuild

## Persistent handoff context

This repository contains the existing Parallax Flow web product in `client/` and its API in `server/`. The current task is to recreate the web UI as a React Native application, preserving real MVP behavior and the product visual identity rather than making a generic course app.

The mobile app will live in a new sibling folder, `mobile/`, so the existing Vite web client remains untouched and can continue to serve as the visual and behavior reference. It uses Expo, TypeScript, Expo Router, TanStack Query, Zustand, and React Native primitives. **The current delivery is UI-only:** local mock data and tappable prototype flows are used while visual design is reviewed. Server API, secure token storage, payments, realtime sessions, and other backend integration are intentionally deferred until UI approval.

### Existing product understood

- **Brand:** Parallax Flow; disciplined, calm, premium exam preparation. Deep navy `#252B69`, indigo `#4654A3`, warm gold `#B38A4A`, Manrope typography, intentional light and dark themes.
- **Student areas:** authentication; Home/Dashboard; Notes (exam → stage → recursive subjects → lessons); secure lesson viewer; Practice; Tracker; Library/orders/receipts; Account.
- **Student navigation:** mobile bottom tabs for Home, Notes, Practice, Tracker, and Library; account is reached from the top bar.
- **Core states:** free/paid/owned/locked lessons, completion/revisions, study check-in/out, countdown/targets/streaks, MCQ and written grading, purchase/coupon/payment states, immutable receipts, loading/empty/error/recovery states, displaced-session handling.
- **Admin:** separate administrative login and content, packages, coupons, questions, settings/broadcast, audit, students, and order workflows. It is desktop-first on the web; a native implementation will make operational screens usable on phone/tablet without inventing unsupported features.
- **Future server compatibility:** the existing client uses an authenticated Axios API client, Bearer access tokens, refresh endpoint, and socket-based single-device-session notification. Those contracts will be addressed only after the UI is finalised.

### Constraints and decisions

- Do not modify or replace `client/`; it is the source of truth for UI and interactions.
- Do not invent features excluded by the existing MVP, including video learning, chat, social feeds, raw secure-PDF downloads, annotations, certificates, or an AI tutor.
- Build in verified vertical slices. Each completed phase updates this file with concrete files, verification, and any decisions made, so another agent can resume without rediscovery.
- UI review must not require the backend. Each screen uses local, realistic Parallax Flow mock content and state transitions; mock data will later be replaced behind a stable UI-facing data boundary.
- Recreate native equivalents; web-only primitives such as DOM/CSS, browser local storage, HTML tables, and browser PDF/canvas cannot be copied verbatim.
- Final platform verification requires a JavaScript runtime/dependencies and, ideally, an Android/iOS emulator or device.

## Phased implementation plan

### Phase 0 — Discovery and project record (completed)

1. Inventory web routes, design tokens, shared UI components, assets, and API contracts.
2. Record the discovered context and phased plan in this file.
3. Confirm workspace cleanliness and decide the native project boundary.

**Exit criteria:** this document reflects the application scope and `mobile/` is confirmed as the implementation target.

### Phase 1 — Native app foundation (completed)

1. Scaffold the Expo + TypeScript app under `mobile/`.
2. Add navigation, query client, state store, local mock-data boundary, theme persistence, font loading, and environment configuration.
3. Implement brand tokens, reusable layout/safe-area handling, and baseline reusable components (button, card, input, badge, loading/empty/error states).
4. Make the visual prototype independently runnable without API configuration.

**Exit criteria:** the app starts, type-checks, and displays branded light/dark foundation screens without a running server.

### Phase 2 — Authentication and student shell (completed)

1. Implement splash, login, signup, OTP verification, password reset, and forced-logout screens with local UI state and demo entry actions.
2. Build native mobile top bar, bottom tabs, account entry, protected routes, and theme toggle.
3. Match validation, loading, error, and accessible control states from the web client.

**Exit criteria:** a reviewer can move through the authentication UI and reach the protected student shell without a backend.

### Phase 3 — Student dashboard and tracker (completed)

1. Recreate the action-oriented Home dashboard: study check-in/out, active timer, exam countdown, target progress, momentum/streak/revisions, notices, and primary actions.
2. Implement Tracker settings and active study-session behavior.
3. Use realistic mock tracker data and state transitions, retaining a replaceable data boundary for future API integration.

**Exit criteria:** tracker UI and state transitions render accurately on device without a server.

### Phase 4 — Notes, catalogue, and secure learning (completed)

1. Implement category/stage selection, recursive subject navigation, lesson rows, states, and lesson/package purchase entry points.
2. Recreate lesson viewer chrome and protected-content messaging with an appropriate native secure-document strategy.
3. Add completion/revision actions and focus-loss recovery where native APIs support it.

**Exit criteria:** reviewers can navigate a representative syllabus and open lessons with accurate visual entitlement states.

### Phase 5 — Practice, commerce, library, and account (completed)

1. Implement practice filters, MCQ answer feedback, written-answer submission/grading result, and statistics.
2. Implement lesson/package checkout, coupon states, payment processing/result surfaces, owned-library list, orders, and receipts.
3. Complete account/profile, avatar upload, phone number, study preference, appearance, and logout flows.

**Exit criteria:** all student tab flows are navigable with local mock data and consistent with the web experience.

### Phase 6 — Admin and production polish (completed for the UI-only prototype)

1. Implement native admin authentication and prioritized operational screens, adapting dense web tables into native lists and forms.
2. Add accessibility, responsive tablet layout, loading/error/empty audit, and platform-specific refinements.
3. Run type-check/lint/build checks, perform a manual visual comparison against `client/`, update this file, and document UI-only run instructions.

**Exit criteria:** the supported app scope builds cleanly, main paths are verified, and handoff documentation is current.

## Change log

| Date | Phase | Change | Verification |
| --- | --- | --- | --- |
| 2026-07-25 | 0 | Created persistent implementation context and phased plan. | Web client routes, design tokens, layouts, and representative page/API code inspected. |
| 2026-07-25 | 1 | Scaffolded `mobile/` as an Expo SDK 57 TypeScript app; added brand tokens, Manrope fonts, persistent theme provider, TanStack Query, Axios API client, auth state, and reusable native UI primitives. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Scope change | Switched delivery to a UI-only prototype with local mock data and no backend startup/configuration requirement. | Future API/auth/payment/realtime work is explicitly deferred until UI approval. |
| 2026-07-25 | 2 | Implemented UI-only login, signup, OTP, password recovery, forced-logout screens, a demo student session, branded student top bar, five-tab navigation, account/theme/logout controls, and representative tab previews. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed; web routes include all auth and student-shell paths. |
| 2026-07-25 | 3 | Recreated the action-oriented student dashboard and Tracker with a shared live mock study session, daily target, exam countdown/pressure, Momentum, streak, revision, syllabus, and Continue Studying/Practice surfaces. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Visual correction | Rebuilt the Home dashboard against the supplied final reference: full wordmark header, moon/avatar controls, proper outline tab icons, layered navy/gold check-in hero, overlapping orb, dotted action and exam-calendar cards, streak, notes CTA, and syllabus card. Added `expo-linear-gradient`, `react-native-svg`, and `lucide-react-native` for native visual fidelity. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Mobile refinement | Tightened the dashboard for phone scale: compacted all non-primary cards and header/nav, subdued background dots, constrained text to professional line breaks, and added spring, pulse, and Android-visible gold-halo feedback to the check-in control. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Hero correction | Reworked the check-in hero to match the supplied active-state reference: responsive 336px wide-screen hero, large layered blue orb, red square checkout affordance, unbroken single-line greeting, session-running copy, and restrained lower gold aura rather than an opaque yellow disc. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Card polish | Redesigned the Notes CTA to avoid overlapping visual elements, set readable dark-on-light syllabus tokens, and fixed action-card height/CTA baseline alignment. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-07-25 | 4 | Built the UI-only Notes experience: selectors, resume context, subject catalogue, lesson free/owned/paid/locked/done/revised states, subject drill-in, protected-reader controls, and purchase context. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed; Notes, lesson, and purchase routes are bundled. |
| 2026-07-25 | 5 | Built local Practice MCQ/written feedback, owned Library and order history, receipts, purchase-success feedback, and expanded Account details/preferences. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed; receipt route is bundled. |
| 2026-07-25 | 6 | Built the UI-only admin workspace: admin entry, overview, content hierarchy, packages, coupons, questions, and audit log; added Account entry point and responsive admin navigation. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed; all admin routes are bundled. |
| 2026-07-25 | Focus-control redesign | Replaced the literal active check-in rendering with an original layered focus-session control: navy glass rings, restrained gold orbit, live-status chip, coral end-session action, and spring/pulse feedback. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Web control parity | Replaced the custom focus-control interpretation with a React Native translation of the existing web `SessionOrb`: target-derived gold progress ring, soft halo/sweep, nested navy shell, check-in/check-out label, round gold/red session button, and live clock. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Session-orb geometry fix | Corrected the React Native translation to use the web orb's measured mobile/large dimensions (196/228px shell and 180/212px ring) within an explicit centered inner canvas. This prevents the SVG progress ring from drifting over the timer and keeps all concentric layers aligned. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-07-25 | Exact SessionOrb hierarchy | Rebuilt the mobile control from the website component hierarchy: 196/228px gradient shell, 180/212px SVG progress ring, inset dashed lens ring, direct centered content, 64px gold/red session button, separately styled elapsed/target time, rotating gold sweep, idle/live glow, and local live clock. Removed the extra inner disc and the stale bare `glow` animation reference that crashed Expo Web. | `npm run lint` and `npx tsc --noEmit` passed with no warnings or errors. |
| 2026-07-25 | Expo Web compatibility | Replaced the progress circle's browser-incompatible SVG `rotation`/`origin` props with canvas rotation and numeric dash arrays, moved pointer events into styles, and disabled the native animation driver only on web. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Notes context cleanup | Removed the fixed UPSC and Prelims 2026 selector pills from Notes and the hardcoded course/category label in subject drill-in. Notes now communicate that they reflect the learner's selected course and category, ready for the selection context to supply catalogue data. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-07-25 | Protected PDF reader | Replaced the fake lesson page/zoom controls with one local five-page sample PDF. The reader scrolls continuously, supports pinch/double-tap zoom, removes manual page navigation and percentage controls, overlays the signed-in email watermark, and enables native screenshot/screen-recording protection while open. Native uses `react-native-pdf`; browser review uses the browser PDF surface. | `npm run lint`, `npx tsc --noEmit`, `npx expo export --platform web`, and `npx expo config --type public` passed. Native PDF rendering requires a development/production build because it includes native code. |
| 2026-07-25 | PDF action hardening | Replaced the browser PDF plugin with an in-app canvas renderer so it exposes no browser download, print, page-navigation, screenshot, or screen-recording controls. The web reader still scrolls and permits normal browser pinch zoom; native capture prevention stays active through Expo ScreenCapture. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Browser PDF worker fix | Replaced Expo-incompatible dynamic worker URL generation with the version-pinned worker source recommended by React-PDF, allowing the canvas reader to load in Expo Web without falling back to the browser PDF plugin. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Reader study actions | Added functional local reader actions: Mark as read toggles completion, Favourite toggles saved state, and Revise increments a revision count while marking the lesson read. State remains available when reopening a lesson in the UI demo. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Learner profile setup | Expanded Account to collect and save core study context: mobile reminder contact, exam name, category/stage, exam date, daily target, reminder time, study language, and timezone. Notes and subject headers now read the saved exam/category from shared local profile state. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Notes library | Rebuilt Notes as a document library with top-three recently opened lessons, a functional favourites area, full-note search, All/In progress/Completed filters, and a separate Notes/Packages switch. Opening a reader records it in Recents; reader favourite/read/revision actions feed the library state. | `npm run lint` and `npx tsc --noEmit` passed. A static web export was attempted but exceeded its build-time limit while compiling the existing PDF worker chunk. |
| 2026-07-25 | Reader action polish | Removed the oversized reader action bar. Mark read, Favourite, and Revise now live in a compact three-dot action sheet on the preceding subject-note list, while reader state still updates the Notes library and Recents. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-07-25 | Notes hierarchy correction | Replaced the flat Notes browsing flow with the intended Subject → Unit → Topic (when applicable) → PDF hierarchy. Notes with no topic level open directly from their unit. Recently opened and favourites remain quick access, and the three-dot study actions now live only beside final PDF rows. | `npm run lint`, `npx tsc --noEmit`, and `npx expo export --platform web` passed. |
| 2026-07-25 | Notes stability fix | Fixed the Web infinite render loop in `NoteActionSheet` by keeping Zustand selectors reference-stable; fallback reader status is now created outside the selector. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-07-25 | Practice session controls | Reworked MCQ Practice into a ten-question session with optional 5/10/15-minute timers, a live countdown, question navigator states (answered, unanswered, marked for review), direct question selection, and an answered-of-total progress bar. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-07-25 | Home learning dashboard | Added a polished 12-week, hours-based consistency heatmap and a three-item Recently opened panel to Home. Each recent item opens the same protected lesson URL as its originating Notes entry; check-in/out, streak, exam countdown, and syllabus cards remain in place. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-07-25 | Home layout simplification | Removed the redundant Continue Studying/Practice pair and Jump into your notes promo from Home. The focused dashboard order is now check-in, exam countdown, consistency, streak, recently opened, and syllabus completion; Notes and Practice remain available in the bottom navigation. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-07-25 | Home study utilities | Added a live Today’s Plan card driven by the same study-session clock as check-in/out, with daily-target progress and a Tracker shortcut. Added a compact Revision Queue that takes the learner directly to the next due PDF. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Dark-theme system redesign | Replaced the indigo-heavy dark palette with a reference-led graphite/near-black system, mint focus accents, restrained gold status accents, and stronger charcoal surface layering. Updated shared cards, controls, header, tabs, Home’s hero/calendar/dotted cards, and Library’s hero so Home, Notes, Practice, Tracker, and Library share the same dark visual language. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Check-in consistency | Replaced Home’s custom decorative session orb with the identical shared `SessionButton` used by Tracker, preserving the Home hero placement while making check-in/check-out visuals and behaviour consistent. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Floating navigation dock | Replaced the flat bottom tab bar with an elevated rounded dock: compact icon-and-label items, a clearly defined active pill, and theme-aware light/graphite surfaces. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Liquid-glass navigation polish | Corrected dark-mode syllabus contrast and upgraded the floating dock to use Expo GlassEffect where available, with a translucent fallback on other platforms, a luminous edge, and translucent active mint state. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Custom Apple-style tab dock | Replaced Expo Router’s default tab-item rendering with a fully controlled glass dock. The new bar uses native GlassEffect, layered translucent gradients, a top sheen, separate unclipped shadow shell, equal-width tab cells, fixed icon wells, consistent label baselines, and a subtle active glass capsule. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Protected-reader visual migration | Removed the legacy indigo PDF-reader shell and migrated the protected lesson page, native loader, and web canvas background to the graphite/mint theme. Added a compact protected badge while preserving continuous scroll, pinch zoom, watermarking, and capture restrictions. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Notes information-architecture redesign | Removed the global Parallax Flow header from Notes and rebuilt the screen around a top search field, account shortcut, Notes/Packages primary tabs, clean file toolbar, status filters, vertical recent/favourite file rows, and a functional list/grid subject browser. Existing Subject → Unit → Topic → PDF navigation and note action menus remain intact. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Notes drill-down consistency | Redesigned Subject, Unit, and Topic pages as one continuous file-browser flow using a shared back/search header, compact breadcrumbs, restrained headings, spacious folder/file rows, counts, and useful empty states. Search now works independently at every hierarchy level, and paid/locked notes correctly route to purchase. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Tracker analytics redesign | Rebuilt Tracker with a graphite/mint live-session hero, cleaner daily-target presentation, a detailed seven-day study-hours bar graph with a two-hour goal line, weekly trend and insight summaries, and polished momentum, exam, and readiness sections. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Header removal and profile access | Removed the persistent Parallax Flow header from every student tab. Added one shared initials-based profile shortcut to each page’s own top-right area—inside Home’s hero and beside the Notes, Practice, Tracker, and Library headings. Theme controls remain in Account. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Home and navigation experience | Rebuilt Home as a cohesive graphite/mint dashboard with an integrated focus-session hero, live daily target, streak/syllabus snapshot, profile-driven exam countdown, structured consistency heatmap, recent-note shortcuts, and revision queue. Moved Profile into a separated avatar destination in the liquid-glass dock, removed page-level avatar duplication, and enabled fade transitions between tabs. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Library and motion redesign | Rebuilt Library as a focused collection dashboard with a protected-access hero, ownership/progress/revision summaries, a clean Lessons/Orders switch, compact progress-aware lesson rows, and polished receipt rows. Removed the existing fade navigation: tabs now use a horizontal shift and deeper student routes use a gesture-enabled slide from the right. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Home content trim | Removed the Home consistency heatmap and the standalone Revision Due card. Home now moves directly from exam countdown to Recently opened, while detailed consistency remains available in Tracker. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Library metric refinement | Replaced the Library's revision-count metric with the total number of pages owned, keeping the summary focused on useful collection information. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Payment summary refinement | Replaced the interim page-count metric and generic order count with payment-relevant Library metrics: Owned, Free Purchases, and Paid Purchases. Free and paid totals are derived from the local order amounts. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Expo Go reader compatibility | Removed the eager `react-native-pdf` import that crashed lesson routing in Expo Go. Expo Go now receives a protected, watermarked five-page continuous-scroll study preview; custom development and production builds dynamically load the real native PDF renderer with pinch/double-tap zoom. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Dark accent brightness | Kept the graphite-black dark foundation but replaced the subdued mint primary (`#80DDBE`) with a brighter electric-blue system (`#7C9CFF` / `#B9C7FF`). Updated shared tokens plus Home, Tracker, Library, profile, protected-reader, and PDF-preview decorative colours for consistent higher-energy contrast. | Confirmed no old mint accent remains; `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Admin redesign — shell and overview | Began the mobile-admin enhancement with a four-destination scope: Overview, Notes, Packages, and Coupons. Replaced the old blue masthead and horizontal six-item menu with a graphite/electric-blue admin identity and liquid-glass bottom dock. Rebuilt Overview with catalogue health, available-note/package totals, explicit mobile permissions, and a primary Create Coupon shortcut. Notes/packages are declared read-only and their publishing remains a website responsibility. | `npm run lint` and `npx tsc --noEmit` passed. Next: rebuild the Notes and Packages views around available content only. |
| 2026-08-01 | Admin requirements and data foundation | Expanded the mobile-admin plan to Overview, Students, Orders/receipts/course analytics, a read-only paid catalogue, and Coupons. Added one shared mock data source covering JEE, CA, and NEET students, study hours, orders, payment metadata, paid notes, packages, and coupon records. Corrected Overview's primary metrics to Total Order Value, Total Students, and Published Notes. Uploading notes or packages remains explicitly unavailable on mobile. | `npm run lint` and `npx tsc --noEmit` passed. Next: Students list/detail and Orders. |
| 2026-08-01 | Admin people and sales analytics | Reorganized the admin dock into Overview, Students, Orders, Catalogue, and Coupons. Added a seven-day paid-order count graph to Overview. Built searchable/course-filterable Students and Orders screens, student detail pages with hours studied and order history, course-wise JEE/CA/NEET order-value analysis, and read-only in-app payment receipts shared by student history and the global Orders list. | `npm run lint` and `npx tsc --noEmit` passed. Next: consolidate paid notes/packages into Catalogue and implement coupon creation. |
| 2026-08-01 | Student/admin navbar parity | Replicated the admin dashboard's cleaner liquid-glass dock on the student dashboard: matching electric-blue glass edge, gradient depth, top sheen, icon wells, spacing, and active states. Profile remains a sixth student destination but now aligns as a normal dock item without a separator or mismatched circular treatment. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Navigation motion removal | Disabled page-transition animation in the root, authentication, student stack, student tabs, and admin stack. Navigation now changes pages immediately without fade, shift, or slide effects. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Subject/topic practice builder | Rebuilt Practice around an explicit Subject → Topic → Format → Timer flow. Added a structured question bank for Indian Polity, Modern History, and Indian Geography; each topic owns its MCQs and written prompt. Starting a session now scopes the navigator, answered count, progress, timer, review marks, answer feedback, and written feedback strictly to the selected topic. | `npm run lint` and `npx tsc --noEmit` passed. Next: mobile admin course/subcategory/subject hierarchy creation. |
| 2026-08-01 | Global zero-motion navigation | Strengthened the earlier transition removal by disabling native stack gestures at every router level, preloading student tabs and retaining inactive screens to prevent mount-time motion, and removing the remaining sliding Notes action modal. Page and modal changes now use no configured animation globally. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Practice active-session summary | Made the large Subject/Topic/Format/Timer builder visible only before a session begins. Once started, it is replaced by a compact live-session dashboard showing selected context, remaining time, answered progress, and review count; written sessions show word count and draft/submitted status. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Admin Courses management | Replaced the generic Catalogue destination with a grouped Courses workspace. Admins can now create Course/Exam → Subcategory/Level → Subject/Phase structures locally, seeded with JEE → Mains/Advanced, CA → Foundation/Intermediate phases, and NEET → UG. A companion Paid Content view lists existing paid notes and packages by course but exposes no mobile upload controls; legacy Packages routes redirect there. All creation sheets and router layouts use no animation. | `npm run lint` and `npx tsc --noEmit` passed; a global scan confirms every router transition is `none` and every remaining modal uses `animationType="none"`. |
| 2026-08-01 | Navigator transition elimination | Removed Stack navigators from the root, authentication, student, and admin layouts and replaced them with direct Expo Router Slots. This eliminates the native transition container itself rather than relying only on `animation: none`; student tab animation remains explicitly disabled. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Notes quick-access carousels | Converted Recently Opened and Favourites from vertical stacks into single-row, snap-aligned horizontal swipe carousels. Quick-access cards keep titles, metadata, status, and revision text to one line with tail truncation, while retaining note opening and three-dot management actions. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Custom Practice countdown | Replaced the hardcoded 5/10/15-minute presets with an alarm-clock-style countdown editor. Learners can type or step custom hours, minutes, and seconds, switch to an untimed session, and see the exact configured countdown in the compact live-session dashboard; zero-length timed sessions are blocked. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Saved-note and timer visual polish | Replaced the misaligned favourite star with a compact bookmark badge anchored to the document icon and updated the action sheet to use the same saved-note language. Rebuilt the custom Practice timer as one unified digital face with editable values, compact minus/plus controls, a proper switch, and a clear configured-time summary. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-01 | Admin complimentary note access | Added a student-scoped free-access workflow to Admin student profiles. Admins can select an available paid note matching the learner's course/category, grant it without an order or receipt, review existing grants, and revoke access. Purchased notes and duplicate grants are excluded. In the local demo flow, matching student accounts immediately see granted notes as unlocked across Notes browsing and reader routing. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | User Home interaction showcase | Rebuilt the student Home page around four premium interactions: a month-navigable streak calendar matching the supplied dark calendar structure, a 365-to-actual exam countdown with eased numeric animation, a continuously flickering/pulsing streak control, and a new grand check-in/check-out control with a breathing halo, active orbit, tactile compression, burst feedback, and live session timer. Page-navigation motion remains disabled; animations are scoped to Home content only. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | Checkout celebration and flame refinement | Added a full-screen checkout appreciation experience based on the supplied reference: night gradient, oversized numbered flame, streak praise, focus reward panel, native Share action, and Claim reward dismissal. Replaced the streak button's jumping motion with a fixed layered flame burn using flicker, sway, glow, and embers. Active streak-calendar days now render as standalone numbered flames with no surrounding boxes. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | Home flame alignment and countdown replay | Removed the remaining circular container behind the streak control so only the layered burning flame and embers remain. Calendar and checkout flame numbers now use independent centered overlay layers with corrected sizing. The exam counter now resets to 365 and animates to the true remaining-day value every time the Home tab gains focus. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | Android streak-flame rendering fix | Removed the rectangular Android elevation/shadow layer that appeared behind the animated Home streak flame. The visual burn now uses only transparent layered SVG flames and embers, preventing a dark box from being composited around the icon. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | Student momentum wallet | Added tappable streak, point, and recovery-heart balances to the Home header, backed by persisted local state. Checkout awards points once daily: 10 below the learner's configured target and 20 when it is met or exceeded. The streak advances on consecutive completions. Recovery hearts are capped and reset to three each calendar month, and the reward panel can spend one heart only when a streak is genuinely broken. Checkout appreciation now displays the earned point reward. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | Calendar activity and countdown pacing | Re-centered calendar flame digits with dedicated single/two-digit sizing, removing the previous downward offset. Added deterministic recent missed-day demo markers and a missed-day legend without introducing day boxes. Changed the Home-focus exam countdown to a short 1.1-second linear run, preserving constant speed while naturally skipping intermediate numbers. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | Phone-safe checkout choreography | Fixed stale-clock negative checkout durations by clamping session time at its source and formatter. Rebuilt the checkout celebration into a staged reference-style sequence—flame entrance, streak-number reveal, message, reward card, then actions—and made the central content scroll-safe on short phones. Removed the Android hardware-shadow aura responsible for the dark broken shape behind the large flame. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | Recently opened rocket micro-animation | Added the supplied reference motion to Home’s Recently opened section as a compact code-native rocket scene. The rocket gently lifts and sways while its gold exhaust pulses, launch trails move, and nearby stars alternate their glow. The header remains responsive on phone widths and note rows retain their existing one-line, tappable behaviour. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | Today’s Plan visual depth | Reworked the plain Today’s Plan widget into a richer daily-focus summary with a target icon, layered orbital background detail, profile-driven target time, live remaining-time tile, twelve-step progress rhythm, momentum label, and retained Tracker shortcut. Removed the enclosing blue tile from the Recently opened rocket so its motion, stars, trails, and orbit details blend directly into the parent card. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | Daily-goal dial and rocket cleanup | Removed the secondary lower-left orbit circle beneath the Recently opened rocket. Upgraded Today’s Plan with a dedicated circular goal dial: its center displays remaining time and its perimeter marker maps to the learner’s live completion percentage, supported by the existing segmented rhythm and momentum state. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Home hero ambient choreography | Recreated the supplied in-card motion as a touch-transparent background layer inside the Home hero: independently rotating orbital systems, moving gold satellites, a counter-rotating eye constellation, drifting particles, and alternating star glints. Removed the oversized translucent yellow breathing circle after phone review. The existing check-in/check-out component, timer, press interaction, and layout remain unchanged. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Tracker visual and motion refinement | Restyled every Tracker area except the protected Study consistency graph. Added an animated analytics mark to the heading; touch-transparent orbit, contour, particle and sparkle motion behind the unchanged session control; a pulsing Today’s Focus gauge and insight; purpose-built streak/momentum signal cards; an animated exam-pressure orbit with milestones; and compact syllabus/revision readiness cards. The consistency chart’s component markup, calculations and styles were not modified. | `npm run lint` and `npx tsc --noEmit` passed. Expo web live rendering was attempted but the local server did not become reachable; no server process was left running. |
| 2026-08-02 | Tracker graduation-cap mark | Replaced the forward/trend arrow inside the Tracker heading badge with a graduation cap. The cap performs a continuous horizontal 3D rotation using perspective and subtle side-on scaling, while the surrounding dotted orbit continues at its own slower speed. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Flame digit optical alignment and cap blending | Repositioned checkout streak digits against the flame’s solid orange body instead of the SVG’s geometric centre, with dedicated one-, two-, and three-digit typography. Applied the same optical right/down alignment and responsive sizing to active calendar flames. Removed the Tracker graduation cap’s outer rounded tile so only the rotating hat and dotted orbit blend into the page heading. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Shared grand session control | Extracted Home’s animated check-in/check-out control into one shared `GrandSessionControl` component and switched both Home and Tracker to that exact implementation. Tracker now has the identical halo, orbit, burst, press response, gradient core, action icon and live timer while retaining its own toggle handler; the Study consistency graph remains untouched. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Raised-orb student navbar | Rebuilt the shared student navbar from the supplied motion reference. A coloured active orb now overlaps the slim glass rail and springs horizontally between tab positions with a pop response, pointer, active-only label, and breathing glow. Each study destination has its own restrained accent; inactive destinations remain quiet icons and the existing Profile shortcut remains available as the sixth item. Global page transitions are still disabled—the movement is confined to the navbar. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Navbar active-orb proportion | Reduced the raised active destination orb from 52px to 42px, scaled its icon and breathing glow proportionally, tightened the glass rail height, and lowered the orb slightly into the rail. The spring movement, destination colours, pointer and active label remain intact without dominating the navbar. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Client-demo screen sharing | Scoped protected-reader capture blocking to route focus so the native secure flag is released immediately when the PDF loses focus instead of waiting for route unmount. Added the explicit `EXPO_PUBLIC_ALLOW_SCREEN_CAPTURE=true` client-demo override, which clears capture and app-switcher protection while presenting; the default and production behaviour remains protected. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Draggable navbar bead | Matched the supplied navbar-motion reference with a rail-bound active bead that can be tapped or dragged horizontally. While dragging, the bead follows the finger, previews each crossed destination through its icon/accent/label, and settles to the nearest tab with a damped spring, compact squash-and-stretch, and fading motion trail. Page content still changes with global route transitions disabled. | `npm run lint` and `npx tsc --noEmit` passed. |
| 2026-08-02 | Student-only navy dark theme | Added a nested student theme so authenticated user routes use the requested `#1C2A41CF` dark canvas while Admin and authentication retain their existing palettes. Rebalanced student card/elevated/sunken surfaces, borders, primary/gold/status accents, foreground/muted/faint typography, hero gradients, protected reader and liquid navbar for clear contrast on the new navy foundation. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Two-stage learner signup | Rebuilt signup with explicit Email and Google methods. Email collects name, mobile, email and password, validates them, and continues through separate six-digit Email OTP and Mobile OTP controls; Google collects name and presents the OAuth email as a locked pre-filled identity with no password. Each OTP channel has independent resend feedback and both must be complete before successful verification. Successful verification/Google creation opens a polished onboarding sheet backed by Admin Courses. Finishing requires course plus exam month/year, accepts an optional day and academy ID, derives the starting category, updates the learner profile, and creates the signed-in identity; Skip for now enters Home without validation and the same profile fields remain editable in Account. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Home daily to-do list | Removed Home’s Recently opened note card and its rocket animation without changing the Notes library’s own recent-history experience. Added a persisted, date-scoped Study to-do list with starter tasks, inline task creation, completion toggles, deletion, clear-completed, empty state, and live completed/total progress. The list automatically starts fresh when the learner opens Home on a new local calendar day. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Premium authentication redesign | Rebuilt the shared Login/Signup presentation with a responsive layered auth canvas, orbital details, glass-style brand/security bar, stronger typography and framed footer. Login now has a focused secure-access card, polished demo entry and concise trust indicators. Signup now presents Email/Google as a clear first step, improves account-form hierarchy, surfaces dual-OTP intent, password strength and privacy cues, while preserving the existing validation, OTP and learner-onboarding flows. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Reference-led dual OTP verification | Rebuilt the verification screen from the supplied motion reference while preserving the required two-channel signup flow. Email and Mobile now have separate six-cell OTP panels, automatic verification after the sixth digit, animated checking glows, green tick transformations, independent resend feedback, and automatic focus handoff from Email to Mobile. A shared secure-device stage and 0/2–2/2 completion indicator communicate overall progress before learner onboarding. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Cinematic welcome and unified authentication | Replaced the root Login redirect with a full-screen, reference-inspired Parallax Flow welcome experience using a warm abstract gradient, focused product message, “Start learning” primary CTA, and existing-account secondary action. Consolidated Sign in and Create account into one `/login` screen with an in-page mode switch; both welcome actions select the appropriate mode without separate-page navigation. Email/Google registration, dual OTP, demo login, forgotten password, and learner onboarding remain intact. The legacy `/signup` URL now redirects to Create account mode instead of rendering its own page. | `npm run lint`, `npx tsc --noEmit`, `npx expo export --platform web`, and `git diff --check` passed. |
| 2026-08-02 | Spacious animated OTP and standard Google signup | Removed the oversized Signup Method selector from the unified Create account mode. The regular name/mobile/email/password form now leads into a familiar OR divider and full-width Sign up with Google action. Rebuilt OTP verification as one spacious sequential Email → Mobile stage rather than two crowded simultaneous forms. Added visible stage slide-ins, digit spring feedback, breathing active-cell feedback, scanning/pulsing verification motion, success-burst rings, springing confirmation ticks, delayed Email-success acknowledgement, automatic Mobile handoff, independent resend state, and persistent 0/2–2/2 progress. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Form-based mock user/admin access | Removed the visible Enter UI demo card from authentication and added a standard Sign in with Google action matching Signup’s Google option. Sign in now accepts an email or demo name: `mockuser` enters the student dashboard and `mockadmin` creates an admin demo session before entering the command centre. In Create account, entering either handle as the name bypasses the remaining mock-data requirements and opens dual OTP with safe demo destinations; after both codes, `mockuser` continues through learner onboarding while `mockadmin` enters Admin directly. Regular account and Google flows remain student-scoped. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Exact-reference OTP composition | Replaced the prior dashboard-style OTP interpretation with the supplied video’s actual single-card composition: black canvas, centred rounded dark card, glowing phone/security illustration, four independent code cells, automatic focus movement, and resend row. Typing the fourth digit now triggers the reference sequence in place—orange/red phone pulse and “Verifying code…”, followed by a full green phone outline, unlocked state, four ticks, Verified button, verified heading and animated loader. The exact card sequence runs first for Email and then resets for Mobile, preserving mock-user onboarding and mock-admin command-centre routing. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | OTP final confirmation tick | Corrected the verified-state timing so the progress spinner is temporary. After 480ms it now transforms into a large springing green confirmation tick with an Email confirmed/Mobile confirmed label, remaining visible before the next OTP stage or destination opens. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | OTP keyboard visibility | Made the reference OTP card keyboard-aware on Android and iOS. Focusing any digit now scrolls the four-cell entry row above the keyboard; completing the fourth digit dismisses the keyboard and restores the card to the top for the verifying and success animations. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |
| 2026-08-02 | Personalisation rocket launch | Added one router-persistent launch-transition provider above the app Slot and routed every learner onboarding completion/skip path through it. After profile setup or Skip for now, a detailed vector rocket rises from below a launch pad, settles into ignition, produces layered pulsing fire, expanding ground smoke and moving exhaust, then accelerates beyond the top of the device. The authenticated Home dashboard mounts while a dark space curtain still covers it; the curtain lifts with the rocket to reveal Home progressively from bottom to top. READY/IGNITION/LIFTOFF copy follows the launch phases. Returning-user login and mock-admin access intentionally remain immediate. | `npm run lint`, `npx tsc --noEmit`, `npx expo export --platform web`, and `git diff --check` passed. |
| 2026-08-02 | Rocket launch smoothness fix | Corrected React Native SVG’s invalid `offset=".55"` warning by using percentage gradient offsets. Moved rocket arrival/lift, vibration, flame, smoke, copy and reveal-curtain transforms/opacity onto the native animation driver on Android/iOS while retaining the JavaScript driver on Web. Home now mounts at 520ms during launch preparation instead of 1180ms during acceleration, allowing the expensive eager tab initialization to finish beneath the opaque curtain without interrupting liftoff frames. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |

| 2026-08-02 | Admin UI refinement phase 1 | Began the UI-only Admin redesign without backend work. Tightened the shared Admin shell for phone widths, replaced the wide online badge with a compact Admin identity control, reduced heading scale and spacing, rebuilt Overview with a smaller command hero, primary/secondary metric hierarchy, four quick-access cards, a cleaner paid-order chart, compact permission/catalogue surfaces, and corrected visible separator characters. The existing Admin liquid-glass navbar remains unchanged. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |

| 2026-08-02 | Admin UI refinement phase 2 | Rebuilt the UI-only Students workspace. The directory now uses a phone-safe learner summary, two compact activity metrics, unified search/course filters, online status, and information-dense learner cards with focus, order, streak and spend indicators. Student details now use a premium profile hero, responsive focus/order cards, an exam and academy learning-plan panel, streak/syllabus/practice study signals, compact complimentary-access and receipt rows, plus a bounded scrollable grant-note sheet. Added realistic local-only profile display fields to the Admin demo records; no backend behavior was introduced. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |

| 2026-08-02 | Admin UI refinement phase 3 | Rebuilt the UI-only Orders experience. Added a phone-safe paid-value hero, compact order-count and average-value cards, locally selectable analytics periods, combined course-distribution and individual comparison bars, unified receipt search/course filters, distinct paid/refunded states, and polished note/package transaction rows. Reworked receipt details into a premium ticket-style status hero, purchased-item summary, structured payment record with edge cut-outs, status-aware totals, and a protected read-only notice; no print, download, payment mutation, or backend behavior was added. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |

| 2026-08-02 | Admin UI refinement phase 4 | Rebuilt the UI-only Courses workspace into two clean modes. Structure now has a compact architecture hero, scrollable course selector, selected-course summary, expandable category/level panels and aligned subject rows, while retaining local Add Course, Category and Subject controls. Catalogue now has a separate website-published summary, Notes/Packages selector, combined search/course filters, clear published/read-only states and phone-sized content rows. Refined creation sheets with hierarchy context and a live display-name preview. Upload, edit and unpublish controls remain intentionally absent on mobile; no backend work was added. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |

| 2026-08-02 | Admin UI refinement phase 5 | Completed the UI-only Admin Coupon and secondary-tools experience. Coupons now have a phone-safe summary, redemption metrics, combined search/status filters, detailed promotion cards, and a keyboard-safe creation sheet with discount type, value, minimum order, course scope, expiry, usage limit, active state, validation and live preview; submitted offers are inserted into local demo state only. Converted the Admin initials control into a tools launcher without changing the five-item navbar. Question Bank is now a read-only expandable view driven by the same local Practice subjects/topics, and Audit Log is a filterable timeline of content, commerce and access events. No backend integration was added. | `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed. |

| 2026-08-02 | Separate sign-in and sign-up journeys | Replaced the unified authentication mode switch with two independent routes and purpose-built screens. The welcome page now sends **Start learning** directly to `/signup` and **I already have an account** directly to `/login`. Sign-in uses a returning-learner visual language and keeps Google, forgotten-password, `mockuser`, and `mockadmin` access. Sign-up uses a distinct onboarding visual language and keeps full identity validation, Google onboarding, dual email/mobile OTP, learner personalisation, and the rocket reveal. Each screen has a small cross-link to the other without combining their forms. | `npm run lint`, `npx tsc --noEmit`, and route-reference audit passed. |

| 2026-08-02 | Removed Admin access intermediary | Removed the visible Command centre access gateway and the Admin demo shortcut from the student Account page. Admin demo entry now exists only through the standard Sign-in form using `mockadmin`, which opens `/admin` directly. The legacy `/admin/login` URL silently redirects to `/login` so saved links do not render a dead or intermediate page. | `npm run lint`, `npx tsc --noEmit`, and route-reference audit passed. |

## Current status and next action

The UI-only prototype is complete through Phase 6. Future work begins only after UI approval: replace local mock data with the server API, secure native session storage, payment integration, real document rendering/security, realtime logout, and production accessibility/device QA.

## Active visual-correction note

On 2026-07-25, reference screenshots established a new visual acceptance target for the student Home dashboard. The existing Phase 3 dashboard is being replaced to match that target: spacious white header, eye/wordmark lock-up, outlined theme control and initials avatar, deep blue gradient hero, large overlapping check-in orb, dotted white/cream cards, two primary action cards, an exam calendar/pressure panel, streak, notes call-to-action, syllabus progress, and a tall icon-led bottom navigation. This reference target takes priority over the earlier compact dashboard composition.
