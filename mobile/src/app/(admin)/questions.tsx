import { StyleSheet, Text, View, Pressable } from 'react-native';
import { CircleHelp, PenLine, Plus } from 'lucide-react-native';
import { AdminShell } from '@/components/admin-shell';
import { Card } from '@/components/ui';
import { font } from '@/constants/theme';
import { useAppTheme } from '@/providers/app-providers';
export default function AdminQuestions() { const { theme } = useAppTheme(); return <AdminShell eyebrow="ASSESSMENT" title="Questions" action={<Pressable style={[styles.add, { backgroundColor: theme.primary }]}><Plus color="#FFF" size={17} /><Text style={styles.addText}>Create</Text></Pressable>}><Card>{[['MCQ', 'Which article protects life and personal liberty?', CircleHelp], ['Short answer', 'Explain the significance of the Preamble.', PenLine]].map(([kind, prompt, Icon]) => { const QuestionIcon = Icon as typeof CircleHelp; return <View key={kind as string} style={[styles.item, { borderBottomColor: theme.line }]}><View style={[styles.icon, { backgroundColor: theme.primarySoft }]}><QuestionIcon color={theme.primary} size={19} /></View><View style={{ flex: 1 }}><Text style={[styles.kind, { color: theme.primary }]}>{kind as string}</Text><Text style={[styles.prompt, { color: theme.fg }]}>{prompt as string}</Text></View></View>; })}</Card></AdminShell>; }
const styles = StyleSheet.create({ add: { minHeight: 36, paddingHorizontal: 10, borderRadius: 10, flexDirection: 'row', alignItems: 'center', gap: 4 }, addText: { color: '#FFF', fontFamily: font.bold, fontSize: 11 }, item: { minHeight: 76, flexDirection: 'row', alignItems: 'center', gap: 10, borderBottomWidth: 1 }, icon: { width: 40, height: 40, borderRadius: 12, alignItems: 'center', justifyContent: 'center' }, kind: { fontFamily: font.bold, fontSize: 10, letterSpacing: .6 }, prompt: { fontFamily: font.medium, fontSize: 12, marginTop: 4, lineHeight: 17 } });
