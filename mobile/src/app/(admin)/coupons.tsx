import { StyleSheet, Text, View, Pressable } from 'react-native';
import { Plus, Tag } from 'lucide-react-native';
import { AdminShell } from '@/components/admin-shell';
import { Card } from '@/components/ui';
import { font } from '@/constants/theme';
import { useAppTheme } from '@/providers/app-providers';
export default function AdminCoupons() { const { theme } = useAppTheme(); return <AdminShell eyebrow="COMMERCE" title="Coupons" action={<Pressable style={[styles.add, { backgroundColor: theme.primary }]}><Plus color="#FFF" size={17} /><Text style={styles.addText}>New</Text></Pressable>}><Card>{[['PRELIMS10', '10% off · Active'], ['POLITY50', '₹50 off · Disabled']].map(([code, detail]) => <View key={code} style={[styles.item, { borderBottomColor: theme.line }]}><View style={[styles.icon, { backgroundColor: theme.goldSoft }]}><Tag color={theme.goldStrong} size={19} /></View><View style={{ flex: 1 }}><Text style={[styles.code, { color: theme.fg }]}>{code}</Text><Text style={[styles.detail, { color: theme.muted }]}>{detail}</Text></View><Text style={[styles.state, { color: detail.includes('Active') ? theme.success : theme.faint }]}>{detail.includes('Active') ? 'Active' : 'Disabled'}</Text></View>)}</Card></AdminShell>; }
const styles = StyleSheet.create({ add: { minHeight: 36, paddingHorizontal: 10, borderRadius: 10, flexDirection: 'row', alignItems: 'center', gap: 4 }, addText: { color: '#FFF', fontFamily: font.bold, fontSize: 11 }, item: { minHeight: 69, flexDirection: 'row', alignItems: 'center', gap: 10, borderBottomWidth: 1 }, icon: { width: 39, height: 39, borderRadius: 12, alignItems: 'center', justifyContent: 'center' }, code: { fontFamily: font.bold, fontSize: 13, letterSpacing: .4 }, detail: { fontFamily: font.regular, fontSize: 11, marginTop: 3 }, state: { fontFamily: font.bold, fontSize: 10 } });
