import { StyleSheet, Text, View, Pressable } from 'react-native';
import { Plus, ChevronRight, FileText } from 'lucide-react-native';
import { AdminShell } from '@/components/admin-shell';
import { Card } from '@/components/ui';
import { font } from '@/constants/theme';
import { useAppTheme } from '@/providers/app-providers';
export default function AdminContent() { const { theme } = useAppTheme(); return <AdminShell eyebrow="CATALOGUE" title="Content" action={<Pressable style={[styles.add, { backgroundColor: theme.primary }]}><Plus color="#FFFFFF" size={17} /><Text style={styles.addText}>Add</Text></Pressable>}><Card><Text style={[styles.label, { color: theme.muted }]}>EXAM HIERARCHY</Text>{['UPSC', 'Prelims 2026', 'Indian Polity', 'Constitutional Framework'].map((entry, index) => <View key={entry} style={[styles.row, { borderTopColor: index ? theme.line : 'transparent', paddingLeft: 8 + index * 14 }]}><FileText size={17} color={index === 3 ? theme.gold : theme.primary} /><Text style={[styles.rowText, { color: theme.fg }]}>{entry}</Text><ChevronRight size={18} color={theme.faint} /></View>)}</Card><Card><Text style={[styles.cardTitle, { color: theme.fg }]}>Selected subject</Text><Text style={[styles.subject, { color: theme.primary }]}>Indian Polity · 12 lessons</Text>{['Constitutional Framework · Published', 'The Preamble · Published', 'Fundamental Rights · Draft'].map((lesson) => <View key={lesson} style={[styles.lesson, { borderTopColor: theme.line }]}><Text style={[styles.rowText, { color: theme.fg }]}>{lesson}</Text><Text style={[styles.status, { color: lesson.includes('Draft') ? theme.goldStrong : theme.success }]}>{lesson.includes('Draft') ? 'Draft' : 'Live'}</Text></View>)}</Card></AdminShell>; }
const styles = StyleSheet.create({ add: { minHeight: 36, paddingHorizontal: 10, borderRadius: 10, flexDirection: 'row', alignItems: 'center', gap: 4 }, addText: { color: '#FFFFFF', fontFamily: font.bold, fontSize: 11 }, label: { fontFamily: font.bold, fontSize: 10, letterSpacing: 1.1 }, row: { minHeight: 46, flexDirection: 'row', alignItems: 'center', gap: 8, borderTopWidth: 1 }, rowText: { flex: 1, fontFamily: font.medium, fontSize: 13 }, cardTitle: { fontFamily: font.bold, fontSize: 16 }, subject: { fontFamily: font.semibold, fontSize: 12, marginTop: 4 }, lesson: { minHeight: 42, borderTopWidth: 1, marginTop: 10, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, status: { fontFamily: font.bold, fontSize: 10 } });
